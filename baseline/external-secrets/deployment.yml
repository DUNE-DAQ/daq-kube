---
deployments:
  - path: namespace
    alwaysDeploy: true

  - barrier: true
    message: We need a namespace to deploy into

  - path: external-secrets
    waitReadiness: true
    tags:
      - secrets

  - barrier: true
    message: Make sure external-secrets is ready (takes about 2m)

  - path: secret-stores
    tags:
      - external-secrets
      - secrets

  - path: ro-user
