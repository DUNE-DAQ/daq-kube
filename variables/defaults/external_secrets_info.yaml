---
external_secrets:
  namespace: external-secrets
  lookup_namespace: external-secrets-storage
  kubernetes_secretStore:
    name: static-passwords
    kind: ClusterSecretStore
  static_password:
    key: static-password
    property: password
  uuid:
    apiVersion: generators.external-secrets.io/v1alpha1
    kind: UUID
    name: uuid

