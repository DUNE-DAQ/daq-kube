---
deployments:
  - path: kube-system
    alwaysDeploy: true

  - path: debug
    alwaysDeploy: true

  - path: common-namespaces
    alwaysDeploy: true

  - path: prometheus-operator-crds
    alwaysDeploy: true
    tags:
      - metrics
      - monitoring
      - alertmanager
      - prometheus
      - prometheus-operator

  - path: cert-manager-crds
    alwaysDeploy: true
    tags:
      - pki
      - tls

  - barrier: true
    message: Prometheus and CertManager CRDs need to settle

  - include: cert-manager
    tags:
      - pki
      - tls

{% if with_cilium %}
  - path: cilium
    tags:
      - cni
    skipDeleteIfTags: true
{% endif %}

  - barrier: true
    message: some cluster bootstrap services need to settle right now or else nothing works

  - include: local-storage
    tags:
      - csi
      - storage

  - barrier: true
    message: bootstrap pods need to settle
