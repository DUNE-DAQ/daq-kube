helmChart:
  chartName: cilium
  chartVersion: 1.19.1
  namespace: '{{ cilium.namespace }}'
  output: helm-rendered.yaml
  releaseName: cilium
  repo: https://helm.cilium.io/
  updateConstraints: ~1.19.x
