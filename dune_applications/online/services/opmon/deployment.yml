---
deployments:
  - path: namespace
    alwaysDeploy: true

  - barrier: true
    message: We need a namespace to deploy into

  - include: database

  - path: opmon-protobuf-dbwriter

vars:
  - file: variables/opmon.yaml
