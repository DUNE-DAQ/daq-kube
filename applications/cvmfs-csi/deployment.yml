---
deployments:
  - path: namespace
    alwaysDeploy: true

  - barrier: true

  - path: cvmfs-csi

  - barrier: true
    message: cvmfs-csi needs to settle

vars:
  - file: variables/cvmfs_csi.yaml
