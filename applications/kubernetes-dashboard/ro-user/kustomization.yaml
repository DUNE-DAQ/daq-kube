apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ro-cluster-role.yaml

namespace: '{{ kubernetes_dashboard.namespace }}'

labels:
  - pairs:
      app.kubernetes.io/component: kubernetes-dashboard
