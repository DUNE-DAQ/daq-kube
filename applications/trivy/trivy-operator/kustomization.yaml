apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helm-rendered.yaml

commonLabels:
  app.kubernetes.io/component: {{ trivy_component_value }}
  app.kubernetes.io/instance: trivy
