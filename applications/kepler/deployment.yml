---
deployments:
  - path: namespace
    alwaysDeploy: true

  - barrier: true
    message: We need a namespace to deploy into

  - path: kepler
  - path: dashboards

vars:
  - file: variables/kepler.yaml
